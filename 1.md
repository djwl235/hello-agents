```markdown
# 环境说明

- Skill 文件均位于 `/home/liuxudong/.openclaw/workspace/skills` 下，其中 `heceAgent` 目录为 Python 脚本专属目录。
- 当前 Docker 挂载卷配置如下：

```yaml
- ${OPENCLAW_CONFIG_DIR}:/home/node/.openclaw   ## openclaw 根目录
- ${OPENCLAW_WORKSPACE_DIR}:/home/node/.openclaw/workspace  ## openclaw 工作空间、日志、skill、模型配置、channel 配置均位于此（详情查看 openclaw.json 文件）
- /home/liuxudong/miniconda3/envs/openclaw:/opt/anaconda3/envs/riskpilot  ## openclaw 执行 python 脚本所用解释器（即此 docker 所用 python 解释器）
- /home/liuxudong/data:/home/liuxudong/data  ## 以下三个包括此均为数据文件夹，可将要处理数据放在这些文件夹下
- /home/liuxudong/kaggle:/home/liuxudong/kaggle
- /home/liuxudong/kaggle1:/home/liuxudong/kaggle1
- /home/liuxudong/datasetlxj:/home/liuxudong/datasetlxj
```

- 目前已打包好的文件：  
  - `openclaw_runtime.tar.gz`（核心组件）  
  - `openclaw_image.tar.gz`（镜像）

---

# 在新机器上还原环境

将生成的 `openclaw_runtime.tar.gz` 和镜像包（`openclaw_image.tar.gz`）传到新机器后，按以下步骤操作。

## 1. 准备目录与解压
为了保证“原封不动”，新机器的用户名最好也叫 `liuxudong`，或者直接解压到根目录 `/` 以保持绝对路径一致：

```bash
# 建议解压到同样的绝对路径，这样一行代码都不用改
sudo tar -xvzf openclaw_runtime.tar.gz -C /
```

## 2. 导入镜像
```bash
docker load < openclaw_image.tar.gz
```

## 3. 解决数据集挂载报错（关键）
因为 `docker-compose.yml` 里写了要挂载 `data`、`kaggle`、`datasetlxj` 等目录（这几个都有数据存放目录，可以在 `docker-compose.yml` 中删除）。在新机器上一键创建这些空文件夹：

```bash
mkdir -p /home/liuxudong/data /home/liuxudong/kaggle /home/liuxudong/kaggle1 /home/liuxudong/datasetlxj
```

# 第三步：一键启动
在新机器的部署目录下执行：

```bash
docker-compose up -d
```